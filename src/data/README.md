# Data

Static content datasets that are not configuration primitives.

Use this folder for long-form editorial content (for example, journal articles) where the structure is stable and consumed by multiple routes.
