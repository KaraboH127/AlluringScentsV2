# Pages

Route-level composition components.

Pages should:
- Assemble reusable components.
- Attach route-specific SEO metadata.
- Keep logic minimal and focused on orchestration.

Pages should not duplicate shared UI logic that can live in `components/` or global state rules that belong in `store/`.
