# Styles

Global style layers that sit alongside Tailwind utilities.

- `theme.css` sets brand-wide defaults such as typography and base colors.

Component-level styling should remain in Tailwind classes inside components.
