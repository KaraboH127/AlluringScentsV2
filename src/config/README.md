# Config

Configuration module(s) that act as application-level source data.

- `site.ts` is the single source of truth for brand information, navigation, collection pricing, fragrance catalogue data, and SEO keyword defaults.

When product or business copy changes, update config first and let pages/components render from this data.
