# Types

Shared TypeScript contracts for content, catalogue data, and component-facing domain models.

Keep interfaces close to business concepts so data in `config/` and `data/` remains strongly typed and predictable.
