# Hooks

Custom React hooks that encapsulate reusable behavior.

Create a hook when behavior:
- Is used in more than one component.
- Requires coordinated setup/cleanup logic.
- Would otherwise clutter component rendering code.

Examples include persistent local storage state and robust body scroll locking for overlays.
